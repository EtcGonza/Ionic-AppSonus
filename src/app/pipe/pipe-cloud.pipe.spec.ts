import { PipeCloudPipe } from './pipe-cloud.pipe';

describe('PipeCloudPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCloudPipe();
    expect(pipe).toBeTruthy();
  });
});
